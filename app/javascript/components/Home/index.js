import React from "react";
import { Button } from "@mui/material";

export default function Home(props){
    return (
        <div>
            <Button 
                variant="contained"
            >
                
            </Button>
        </div>
    );
}